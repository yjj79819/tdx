# 问财技能库集成开发文档

## 一、项目目标

基于同花顺问财技能库，构建功能更强大的股票推荐系统，不依赖现有项目，独立开发新版本。

---

## 二、问财技能库可用能力

### 2.1 核心数据技能

| 技能名称 | 功能描述 | 应用场景 |
|---------|---------|---------|
| **实时行情** | 获取股票实时价格、涨跌幅、成交量、主力资金流向、技术指标 | 实时股价更新、资金流向监控 |
| **A股筛选** | 自然语言筛选股票，支持行情、技术形态、财务、行业概念多条件组合 | 热门股票筛选、条件选股 |
| **板块筛选** | 行业估值、资金流向、涨跌幅、板块类型筛选 | 板块轮动分析 |
| **指数行情** | 上证、沪深300、创业板指等指数数据 | 大盘走势参考 |
| **财务数据** | 营业收入、净利润、ROE、负债率、现金流等 | 基本面分析 |
| **机构观点** | 研报评级、业绩预测、ESG评级、券商金股 | 机构动向跟踪 |

### 2.2 技术面分析技能

| 技能名称 | 功能描述 |
|---------|---------|
| **K线形态识别** | 15种经典K线形态（单根、双根、三根、趋势确认） |
| **技术指标集合** | EMA、ADX、布林带、RSI、OBV、量比等 |
| **一目均衡表** | 转折线、基准线交叉、云图位置、迟行线确认 |
| **缠论形态** | 分型、笔、中枢、买卖点识别 |
| **谐波形态** | Gartley、Bat、Butterfly、Crab等XABCD结构 |

### 2.3 消息面/政策面技能

| 技能名称 | 功能描述 |
|---------|---------|
| **资讯搜索** | 官媒、财经媒体、行业网站、企业官网资讯 |
| **公告查询** | 定期报告、分红派息、回购增持、资产重组 |
| **研报查询** | 投研机构研究报告、投资评级、目标价 |
| **事件数据** | 业绩预告、增发、质押、解禁、调研、监管函 |
| **舆情监控** | 社交媒体情绪、恐慌贪婪指数 |

### 2.4 资金面技能

| 技能名称 | 功能描述 |
|---------|---------|
| **北向资金** | 沪深港通资金流向、板块配置变化 |
| **主力资金** | 大单、中单、小单流向分析 |
| **大宗交易** | 大宗交易数据挖掘 |
| **龙虎榜** | 游资、机构席位追踪 |

---

## 三、推荐因子体系设计

### 3.1 多因子模型架构

```
总评分 = w1*热度因子 + w2*技术因子 + w3*基本面因子 + w4*资金因子 + w5*消息因子
```

### 3.2 热度因子（现有能力增强）

```python
# 问财查询示例
queries = [
    "今日热门股票前200名",
    "连续3天进入热度前50的股票",
    "热度排名上升最快的股票"
]
```

**因子计算**：
- 当前热度排名
- 热度变化趋势（3日/5日）
- 热度稳定性（连续上榜天数）

### 3.3 技术面因子（新增）

```python
# 问财查询示例
queries = [
    "MACD金叉且RSI<30的股票",      # 超卖反弹信号
    "突破布林带上轨的股票",         # 强势突破
    "KDJ金叉且成交量放大的股票",   # 量价配合
    "出现早晨之星形态的股票",       # K线形态
    "5日均线上穿20日均线的股票"    # 均线金叉
]
```

**因子计算**：
- MACD信号（金叉/死叉）
- RSI超买超卖
- 布林带位置
- K线形态得分
- 均线排列状态
- 成交量异动

### 3.4 基本面因子（新增）

```python
# 问财查询示例
queries = [
    "PE<20且ROE>15%的股票",        # 低估值高盈利
    "净利润连续3年增长>20%的股票",  # 高成长
    "负债率<50%且现金流为正的股票", # 财务健康
    "股息率>3%的股票"              # 高分红
]
```

**因子计算**：
- 估值因子（PE、PB、PS）
- 盈利因子（ROE、ROA、毛利率）
- 成长因子（营收增速、利润增速）
- 财务因子（负债率、现金流）
- 分红因子（股息率）

### 3.5 资金面因子（新增）

```python
# 问财查询示例
queries = [
    "北向资金连续3日净流入的股票",
    "主力资金净流入前50名",
    "大单净买入占比>30%的股票",
    "机构席位净买入的股票"
]
```

**因子计算**：
- 北向资金流向
- 主力资金净额
- 大单占比
- 龙虎榜机构买入

### 3.6 消息面因子（新增）

```python
# 问财查询示例
queries = [
    "最近3天有研报给出买入评级的股票",
    "有重大利好公告的股票",
    "机构调研家数>10的股票",
    "所属板块有政策利好的股票"
]
```

**因子计算**：
- 研报评级变化
- 公告利好/利空
- 机构调研热度
- 政策影响评分
- 舆情情绪得分

---

## 四、问财API调用方式

### 4.1 基础接口

```
GET https://www.iwencai.com/unifiedwap?question={query}
```

### 4.2 常用查询模板

```python
IWENCAI_QUERIES = {
    # 热度相关
    "hot_stocks_top200": "今日热门股票前200名",
    "hot_stocks_rising": "热度排名上升最快的50只股票",
    
    # 技术面
    "macd_golden_cross": "MACD金叉的股票",
    "rsi_oversold": "RSI小于30的股票",
    "break_bollinger": "突破布林带上轨的股票",
    "volume_surge": "成交量放大2倍以上的股票",
    
    # 基本面
    "low_pe_high_roe": "PE小于20且ROE大于15%的股票",
    "high_growth": "净利润增长率大于30%的股票",
    "high_dividend": "股息率大于4%的股票",
    
    # 资金面
    "north_inflow": "北向资金净流入前50名",
    "main_inflow": "主力资金净流入前50名",
    "institution_buy": "龙虎榜机构净买入的股票",
    
    # 消息面
    "research_buy": "最近一周有研报买入评级的股票",
    "major_event": "最近3天有重大公告的股票",
    "policy_benefit": "所属板块有政策利好的股票",
    
    # 组合筛选
    "tech_fund_combo": "MACD金叉且主力资金净流入的股票",
    "value_growth": "PE<25且净利润增速>20%的股票"
}
```

### 4.3 返回数据结构

```json
{
    "data": {
        "stock_list": [
            {
                "code": "000001",
                "name": "平安银行",
                "price": 10.50,
                "change_pct": 2.35,
                "volume": 12345678,
                "amount": 129523456.78,
                "turnover_rate": 1.23,
                "pe": 6.5,
                "pb": 0.8,
                "roe": 12.5,
                "main_inflow": 56789000,
                "north_inflow": 12345000,
                ...
            }
        ],
        "total": 200,
        "query_time": "2026-05-28 12:00:00"
    }
}
```

---

## 五、系统架构设计

### 5.1 模块划分

```
iwencai_stock_system/
├── api/
│   ├── iwencai_client.py      # 问财API客户端
│   └── query_builder.py       # 查询语句构建器
├── factors/
│   ├── hot_factor.py          # 热度因子
│   ├── tech_factor.py         # 技术面因子
│   ├── fundamental_factor.py  # 基本面因子
│   ├── capital_factor.py      # 资金面因子
│   └── news_factor.py         # 消息面因子
├── engine/
│   ├── scorer.py              # 因子评分引擎
│   ├── recommender.py         # 推荐引擎
│   └── filter.py              # 过滤器
├── data/
│   ├── cache.py               # 数据缓存
│   └── storage.py             # 数据存储
├── web/
│   ├── app.py                 # Flask应用
│   └── templates/             # 前端模板
└── config/
    └── settings.py            # 配置文件
```

### 5.2 数据流

```
用户请求 → 问财API → 多因子计算 → 综合评分 → 排序筛选 → 返回推荐
                ↓
            缓存层（Redis/文件）
```

### 5.3 更新策略

| 数据类型 | 更新频率 | 说明 |
|---------|---------|------|
| 实时行情 | 1-5分钟 | 交易时段高频更新 |
| 热度排名 | 10分钟 | 问财热度数据 |
| 技术指标 | 15分钟 | K线、均线等 |
| 资金流向 | 30分钟 | 北向、主力资金 |
| 基本面 | 每日 | 财务数据变化慢 |
| 消息面 | 实时 | 公告、研报、舆情 |

---

## 六、推荐算法流程

### 6.1 主流程

```python
def recommend_stocks(user_preferences):
    # 1. 获取候选池（热度前200）
    candidates = get_hot_stocks_top200()
    
    # 2. 多因子评分
    scores = {}
    for stock in candidates:
        scores[stock['code']] = {
            'hot': calc_hot_factor(stock),
            'tech': calc_tech_factor(stock),
            'fund': calc_fundamental_factor(stock),
            'capital': calc_capital_factor(stock),
            'news': calc_news_factor(stock)
        }
    
    # 3. 加权综合评分
    weights = user_preferences.get('weights', DEFAULT_WEIGHTS)
    for code, factor_scores in scores.items():
        total = sum(weights[k] * v for k, v in factor_scores.items())
        scores[code]['total'] = total
    
    # 4. 过滤（ST、亏损、价格等）
    filtered = apply_filters(candidates, scores)
    
    # 5. 排序返回
    result = sorted(filtered, key=lambda x: x['total'], reverse=True)
    return result[:50]  # 返回前50只
```

### 6.2 因子权重配置

```python
DEFAULT_WEIGHTS = {
    'hot': 0.25,      # 热度因子权重
    'tech': 0.20,     # 技术面权重
    'fund': 0.20,     # 基本面权重
    'capital': 0.20,  # 资金面权重
    'news': 0.15      # 消息面权重
}

# 不同风格配置
STYLE_CONFIGS = {
    '短线': {'hot': 0.35, 'tech': 0.30, 'fund': 0.10, 'capital': 0.15, 'news': 0.10},
    '中线': {'hot': 0.20, 'tech': 0.20, 'fund': 0.25, 'capital': 0.20, 'news': 0.15},
    '长线': {'hot': 0.10, 'tech': 0.15, 'fund': 0.35, 'capital': 0.20, 'news': 0.20},
    '跟庄': {'hot': 0.20, 'tech': 0.15, 'fund': 0.15, 'capital': 0.35, 'news': 0.15}
}
```

---

## 七、前端功能设计

### 7.1 页面模块

1. **热门排行** - 热度排名、涨跌幅、资金流向
2. **技术选股** - 技术形态、指标信号
3. **基本面筛选** - 估值、盈利、成长筛选
4. **资金追踪** - 北向资金、主力资金、龙虎榜
5. **消息监控** - 公告、研报、舆情
6. **综合推荐** - 多因子综合评分推荐

### 7.2 交互功能

- 风格切换（短线/中线/长线/跟庄）
- 因子权重自定义
- 条件组合筛选
- 历史回测验证

---

## 八、开发计划

### Phase 1: 基础框架（1-2天）
- [ ] 问财API客户端封装
- [ ] 数据缓存机制
- [ ] 基础Flask应用

### Phase 2: 因子开发（2-3天）
- [ ] 热度因子
- [ ] 技术面因子
- [ ] 基本面因子
- [ ] 资金面因子
- [ ] 消息面因子

### Phase 3: 推荐引擎（1-2天）
- [ ] 因子评分引擎
- [ ] 加权综合算法
- [ ] 过滤器

### Phase 4: 前端开发（2-3天）
- [ ] 页面布局
- [ ] 数据可视化
- [ ] 交互功能

### Phase 5: 测试优化（1-2天）
- [ ] 回测验证
- [ ] 性能优化
- [ ] 部署上线

---

## 九、与现有项目对比

| 功能 | 现有版本 | 新版本 |
|-----|---------|-------|
| 数据源 | 同花顺热榜API + 新浪股价 | 问财技能库（更全面） |
| 推荐因子 | 热度为主 | 多因子（热度+技术+基本面+资金+消息） |
| 技术分析 | 无 | K线形态、技术指标、均线系统 |
| 基本面 | 仅PE | 估值+盈利+成长+财努+分红 |
| 资金面 | 无 | 北向资金+主力资金+龙虎榜 |
| 消息面 | 无 | 公告+研报+舆情+政策 |
| 筛选能力 | 固定条件 | 自然语言灵活筛选 |
| 风格配置 | 无 | 短线/中线/长线/跟庄 |

---

## 十、注意事项

1. **API限流** - 问财API可能有调用频率限制，需合理控制
2. **数据缓存** - 高频数据需要缓存，避免重复请求
3. **因子有效性** - 需要历史回测验证各因子有效性
4. **权重优化** - 不同市场环境下因子权重可能需要调整
5. **独立部署** - 新项目独立部署，不影响现有系统

---

## 附录：问财技能完整列表

详见：https://www.iwencai.com/skillhub

核心技能ID：
- `stock_quote` - 实时行情
- `stock_screen` - A股筛选
- `sector_screen` - 板块筛选
- `financial_data` - 财务数据
- `institution_view` - 机构观点
- `event_data` - 事件数据
- `capital_flow` - 资金流向
