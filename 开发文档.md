# 同花顺热度排名系统 - 开发文档

## 一、项目概述

### 1.1 项目目标
构建一个功能完善的股票热度排名分析系统，整合同花顺热度数据、板块概念信息、实时行情数据，提供可视化的分析界面。

### 1.2 核心价值
- **涨停原因分析**：相同涨停原因、板块、概念会影响个股未来走势
- **板块热度追踪**：识别热门板块，发现板块内优质个股
- **资金动向监控**：通过热度排名追踪市场关注度变化

---

## 二、数据来源与获取方式

### 2.1 数据源总览

| 数据源 | 类型 | 获取方式 | 更新频率 | 数据内容 |
|--------|------|----------|----------|----------|
| 同花顺热榜API | 官方API | HTTP请求 | 每小时 | 热度排名、股票基础信息 |
| levistock库 | 第三方库 | Python SDK | 实时 | 板块、概念、行情数据 |
| 东方财富 | 第三方 | levistock封装 | 实时 | 实时行情、财务数据 |
| 同花顺页面 | 网页爬虫 | requests+bs4 | 每日 | 涨停原因、连板数等 |

### 2.2 同花顺热榜API

**API地址**：
```
https://dq.10jqka.com.cn/fuyao/hot_list_data/out/hot_list/v1/stock?stock_type=a&type=hour&list_type=normal
```

**请求头**：
```python
headers = {
    'User-Agent': 'Mozilla/5.0 (iPhone; CPU iPhone OS 16_0 like Mac OS X) AppleWebKit/605.1.15',
    'Accept': 'application/json',
}
```

**返回数据字段**：
| 字段 | 类型 | 说明 |
|------|------|------|
| code | string | 股票代码（6位数字） |
| name | string | 股票名称 |
| order | int | 热度排名（1-100） |
| hot_value | int | 热度值 |

**获取代码示例**：
```python
import requests

def fetch_hot_rank():
    url = 'https://dq.10jqka.com.cn/fuyao/hot_list_data/out/hot_list/v1/stock?stock_type=a&type=hour&list_type=normal'
    headers = {
        'User-Agent': 'Mozilla/5.0 (iPhone; CPU iPhone OS 16_0 like Mac OS X) AppleWebKit/605.1.15',
        'Accept': 'application/json',
    }
    resp = requests.get(url, headers=headers, timeout=15)
    data = resp.json()
    if data.get('status_code') == 0:
        return data['data'].get('stock_list', [])
    return []
```

### 2.3 levistock 数据获取

**项目地址**：https://github.com/fleetinglife/levistock

**安装**：
```bash
pip install levistock
```

#### 2.3.1 热榜数据接口
```python
import levistock as lk

# 获取同花顺热榜TOP100
hot_rank = lk.stock_hot_rank_ths()
# 返回：DataFrame，包含 code, name, rank 等字段
```

#### 2.3.2 板块数据接口
```python
# 获取股票所属行业板块
codes = ['600584', '002185', '002156']
belong = lk.sector_stock_belong_em(codes)
# 返回：[{'stock_code': '600584', 'sector_name': '半导体'}, ...]

# 获取所有行业板块列表
industry_sectors = lk.sector_em()
# 返回：DataFrame，包含板块名称、涨跌幅等

# 获取所有概念板块列表
concept_sectors = lk.sector_em(sector_type="concept")
# 返回：DataFrame，包含概念名称、涨跌幅等
```

#### 2.3.3 实时行情接口
```python
# 获取所有A股实时行情
spot_df = lk.stock_zh_a_spot_em()

# 返回字段说明：
# 代码、名称、最新价、涨跌幅、涨跌额、成交量、成交额、振幅、
# 最高、最低、今开、昨收、量比、换手率、市盈率-动态、市净率、
# 总市值、流通市值、涨速、5分钟涨跌、60日涨跌幅、年初至今涨跌幅
```

#### 2.3.4 个股详细信息
```python
# 获取个股详细信息（含概念）
info = lk.stock_individual_info_em('600584')
# 返回：包含所属概念、行业、地区等信息
```

### 2.4 概念数据获取方案

由于 levistock 的概念数据获取较复杂，采用以下方案：

**方案1：通过 sector_stock_belong_em 获取**
```python
# 获取股票所属板块（包含概念）
belong = lk.sector_stock_belong_em(codes)
# 返回板块名称，部分包含概念信息
```

**方案2：通过 stock_individual_info_em 获取（推荐）**
```python
def fetch_concepts(codes):
    """获取股票概念信息"""
    concepts = {}
    for code in codes:
        try:
            info = lk.stock_individual_info_em(code)
            if info is not None and len(info) > 0:
                # 从info中提取概念
                concept = info.get('概念', '') if isinstance(info, dict) else ''
                concepts[code] = concept
        except:
            pass
    return concepts
```

**方案3：从同花顺页面抓取**
```python
import requests
from bs4 import BeautifulSoup

def fetch_concept_from_ths(code):
    """从同花顺个股页面抓取概念"""
    url = f"https://stockpage.10jqka.com.cn/{code}/"
    headers = {'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)'}
    resp = requests.get(url, headers=headers)
    soup = BeautifulSoup(resp.text, 'html.parser')
    # 解析概念标签
    concept_tags = soup.select('.concept-tags .tag')
    return [tag.text for tag in concept_tags]
```

### 2.5 涨停原因等字段获取

**数据来源**：同花顺热榜详情页面

**API地址**：
```
https://basic.10jqka.com.cn/api/stockph/briefinfo?code={code}
```

**获取代码**：
```python
def fetch_limit_up_info(code):
    """获取涨停原因、连板数等信息"""
    url = f"https://basic.10jqka.com.cn/api/stockph/briefinfo?code={code}"
    headers = {
        'User-Agent': 'Mozilla/5.0 (iPhone; CPU iPhone OS 16_0 like Mac OS X)',
        'Referer': 'https://eq.10jqka.com.cn/'
    }
    try:
        resp = requests.get(url, headers=headers, timeout=10)
        data = resp.json()
        return {
            'limit_up_reason': data.get('limit_up_reason', ''),  # 涨停原因
            'consecutive_boards': data.get('consecutive_boards', 0),  # 连板数
            'first_limit_time': data.get('first_limit_time', ''),  # 首次涨停时间
            'seal_amount': data.get('seal_amount', 0),  # 封单金额
            'national_fund': data.get('national_fund', '')  # 国家大基金持股
        }
    except:
        return {}
```

---

## 三、数据字段详细说明

### 3.1 基础信息字段

| 字段名 | 数据来源 | 获取方式 | 说明 |
|--------|----------|----------|------|
| code | 同花顺API | HTTP请求 | 6位数字股票代码 |
| name | 同花顺API | HTTP请求 | 股票中文名称 |
| current_rank | 同花顺API | HTTP请求 | 当前热度排名（1-100） |
| market | 代码前缀判断 | 程序计算 | 17=沪市，33=深市 |

### 3.2 板块概念字段

| 字段名 | 数据来源 | 获取方式 | 说明 |
|--------|----------|----------|------|
| sector | levistock | sector_stock_belong_em() | 所属行业板块（如：半导体） |
| concept | levistock | stock_individual_info_em() | 所属概念（如：芯片概念,国家大基金持股） |

**板块与概念的区别**：
- **板块（Sector）**：行业分类，如半导体、银行、医药等，一个股票只属于一个行业板块
- **概念（Concept）**：主题分类，如芯片概念、人工智能、新能源等，一个股票可属于多个概念

### 3.3 实时行情字段

| 字段名 | 数据来源 | 获取方式 | 说明 | 单位 |
|--------|----------|----------|------|------|
| price | levistock | stock_zh_a_spot_em() | 最新价 | 元 |
| change_pct | levistock | stock_zh_a_spot_em() | 涨跌幅 | % |
| change_amt | levistock | stock_zh_a_spot_em() | 涨跌额 | 元 |
| volume | levistock | stock_zh_a_spot_em() | 成交量 | 手 |
| turnover | levistock | stock_zh_a_spot_em() | 成交额 | 元 |
| turnover_rate | levistock | stock_zh_a_spot_em() | 换手率 | % |
| pe_ratio | levistock | stock_zh_a_spot_em() | 市盈率-动态 | 倍 |
| market_cap | levistock | stock_zh_a_spot_em() | 总市值 | 元 |

### 3.4 涨停相关字段

| 字段名 | 数据来源 | 获取方式 | 说明 | 用途 |
|--------|----------|----------|------|------|
| limit_up_reason | 同花顺API | 页面抓取 | 涨停原因 | 分析涨停驱动因素 |
| national_fund | 同花顺API | 页面抓取 | 国家大基金持股 | 识别国家队持仓 |
| consecutive_boards | 同花顺API | 页面抓取 | 连板数 | 衡量涨停强度 |
| first_limit_time | 同花顺API | 页面抓取 | 首次涨停时间 | 判断涨停早晚 |
| seal_amount | 同花顺API | 页面抓取 | 封单金额 | 衡量封板力度 |

---

## 四、数据库设计

### 4.1 表结构

```sql
-- 股票基础信息表
CREATE TABLE stocks (
    code TEXT PRIMARY KEY,          -- 股票代码
    name TEXT,                      -- 股票名称
    market TEXT,                    -- 市场代码（17=沪市，33=深市）
    current_rank INTEGER,           -- 当前热度排名
    sector TEXT,                    -- 所属行业板块
    concept TEXT,                   -- 所属概念（逗号分隔）
    price REAL,                     -- 最新价
    change_pct REAL,                -- 涨跌幅%
    change_amt REAL,                -- 涨跌额
    volume REAL,                    -- 成交量
    turnover REAL,                  -- 成交额
    turnover_rate REAL,             -- 换手率%
    pe_ratio REAL,                  -- 市盈率
    market_cap REAL,                -- 总市值
    limit_up_reason TEXT,           -- 涨停原因
    national_fund TEXT,             -- 国家大基金持股
    consecutive_boards INTEGER,     -- 连板数
    first_limit_time TEXT,          -- 首次涨停时间
    seal_amount REAL,               -- 封单金额
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 热度排名历史表
CREATE TABLE hot_rank_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    code TEXT,
    date TEXT,
    rank INTEGER,
    price REAL,
    change_pct REAL,
    UNIQUE(code, date)
);

-- 板块热度统计表
CREATE TABLE sector_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date TEXT,
    sector TEXT,
    stock_count INTEGER,            -- 上榜股票数
    avg_change_pct REAL,            -- 平均涨跌幅
    total_turnover REAL,            -- 总成交额
    hot_stocks TEXT                 -- 热门股票JSON
);

-- 涨停原因统计表
CREATE TABLE limit_up_reason_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date TEXT,
    reason TEXT,                    -- 涨停原因
    stock_count INTEGER,            -- 涉及股票数
    stocks TEXT,                    -- 股票列表JSON
    avg_next_day_return REAL        -- 次日平均收益
);
```

### 4.2 索引设计

```sql
-- 提高查询效率
CREATE INDEX idx_hot_rank_code ON hot_rank_history(code);
CREATE INDEX idx_hot_rank_date ON hot_rank_history(date);
CREATE INDEX idx_stocks_sector ON stocks(sector);
CREATE INDEX idx_stocks_concept ON stocks(concept);
CREATE INDEX idx_sector_stats_date ON sector_stats(date);
```

---

## 五、核心算法与逻辑

### 5.1 数据更新流程

```
开始更新
  │
  ├─ 1. 获取热榜TOP100（同花顺API）
  │     └─ 获取 code, name, rank
  │
  ├─ 2. 获取板块信息（levistock）
  │     └─ 调用 sector_stock_belong_em(codes)
  │
  ├─ 3. 获取实时行情（levistock）
  │     └─ 调用 stock_zh_a_spot_em()
  │     └─ 筛选热榜股票的行情数据
  │
  ├─ 4. 获取概念信息（可选）
  │     └─ 调用 stock_individual_info_em()
  │
  ├─ 5. 获取涨停原因（同花顺页面）
  │     └─ 调用 fetch_limit_up_info()
  │
  ├─ 6. 合并数据
  │     └─ 按 code 关联所有字段
  │
  └─ 7. 写入数据库
        ├─ 更新 stocks 表
        └─ 插入 hot_rank_history 表
```

### 5.2 板块热度分析算法

```python
def analyze_sectors(stocks):
    """
    分析板块热度
    
    输入：TOP100股票列表
    输出：按上榜数量排序的板块列表
    """
    sector_stats = {}
    
    for stock in stocks:
        sector = stock['sector']
        if sector not in sector_stats:
            sector_stats[sector] = {
                'count': 0,
                'stocks': [],
                'changes': []
            }
        sector_stats[sector]['count'] += 1
        sector_stats[sector]['stocks'].append(stock)
        sector_stats[sector]['changes'].append(stock['change_pct'])
    
    # 计算平均涨跌幅
    for sector in sector_stats:
        changes = sector_stats[sector]['changes']
        sector_stats[sector]['avg_change'] = sum(changes) / len(changes)
    
    # 按上榜数量排序
    return sorted(sector_stats.items(), key=lambda x: -x[1]['count'])
```

### 5.3 涨停原因归类算法

```python
def analyze_limit_up_reasons(stocks):
    """
    涨停原因归类分析
    
    核心价值：相同涨停原因的股票往往有相似的未来走势
    """
    reason_stats = {}
    
    for stock in stocks:
        reason = stock.get('limit_up_reason', '')
        if not reason:
            continue
            
        # 提取关键词（简化处理）
        keywords = extract_keywords(reason)
        
        for keyword in keywords:
            if keyword not in reason_stats:
                reason_stats[keyword] = {
                    'count': 0,
                    'stocks': [],
                    'sectors': set()
                }
            reason_stats[keyword]['count'] += 1
            reason_stats[keyword]['stocks'].append(stock)
            reason_stats[keyword]['sectors'].add(stock['sector'])
    
    return reason_stats
```

---

## 六、UI/UX 设计

### 6.1 暗色系配色方案

```css
:root {
  --bg-primary: #0f0f23;       /* 主背景 */
  --bg-card: #1a1a2e;          /* 卡片背景 */
  --bg-hover: #252542;         /* 悬停背景 */
  --text-primary: #ffffff;     /* 主文字 */
  --text-secondary: #a0a0a0;   /* 次要文字 */
  --accent-primary: #00d4ff;   /* 主色调（青色） */
  --accent-danger: #ff4757;    /* 红色/涨 */
  --accent-success: #2ed573;   /* 绿色/跌 */
  --accent-warning: #ffa502;   /* 橙色/警告 */
  --border-color: #2d2d44;     /* 边框 */
}
```

### 6.2 布局结构

```
┌─────────────────────────────────────────────────────────────────┐
│  🔥 同花顺热度排名                              股票:1316 记录:452430 │
├─────────────────────────────────────────────────────────────────┤
│  📅 选择日期 [2026-05-25 ▼]  [刷新] [🔄 立即更新]                      │
├─────────────────────────────────────────────────────────────────┤
│  [📊 热度排名] [🏭 板块分析] [💡 概念分析] [🔥 涨停原因]              │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 排名 │ 股票 │ 价格 │ 涨跌幅 │ 板块 │ 概念 │ 涨停原因 │ 连板 │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │  1   │长电  │35.68 │+10.02%│半导体│芯片  │ 芯片涨价 │  2   │  │
│  │  2   │华天  │28.50 │ +8.50%│半导体│封测  │ 业绩预增 │  1   │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 6.3 交互设计

#### 6.3.1 表格排序
- 点击表头触发排序
- 首次点击升序，再次点击降序
- 显示排序箭头图标 (↑ ↓)
- 红色箭头=涨序，绿色箭头=跌序

#### 6.3.2 数据筛选
- 板块筛选下拉框
- 概念标签云筛选
- 涨跌幅范围滑块

#### 6.3.3 涨停原因高亮
- 相同涨停原因的股票行高亮显示
- 鼠标悬停显示详细原因
- 点击原因查看所有相关股票

---

## 七、API 接口设计

### 7.1 获取TOP50（含完整字段）

```
GET /api/top50/<date>
```

**Response**：
```json
[
  {
    "code": "600584",
    "name": "长电科技",
    "rank": 1,
    "sector": "半导体",
    "concept": "芯片概念,国家大基金持股",
    "price": 35.68,
    "change_pct": 10.02,
    "change_amt": 3.25,
    "volume": 1250000,
    "turnover": 44500000,
    "turnover_rate": 5.68,
    "limit_up_reason": "芯片涨价",
    "national_fund": "是",
    "consecutive_boards": 2
  }
]
```

### 7.2 板块分析

```
GET /api/sector/<date>
```

**Response**：
```json
[
  {
    "sector": "半导体",
    "count": 17,
    "avg_change": 5.23,
    "stocks": [
      {"code": "600584", "name": "长电科技", "rank": 1},
      {"code": "002185", "name": "华天科技", "rank": 2}
    ]
  }
]
```

### 7.3 概念分析

```
GET /api/concept/<date>
```

**Response**：
```json
[
  {
    "concept": "芯片概念",
    "count": 12,
    "stocks": [...]
  },
  {
    "concept": "国家大基金持股",
    "count": 8,
    "stocks": [...]
  }
]
```

### 7.4 涨停原因分析

```
GET /api/limit_up_reason/<date>
```

**Response**：
```json
[
  {
    "reason": "AI算力",
    "count": 8,
    "stocks": [...],
    "sectors": ["半导体", "元件"]
  }
]
```

---

## 八、开发计划

### 8.1 第一阶段：基础功能（已完成）
- [x] 热榜数据采集
- [x] 板块信息获取
- [x] 基础Web界面

### 8.2 第二阶段：UI改造（进行中）
- [x] 暗色系CSS样式
- [x] 按钮布局调整（刷新和立即更新放一行）
- [x] 扁平化设计
- [x] 表格排序功能

### 8.3 第三阶段：数据扩展
- [x] 数据库表结构更新
- [x] 整合 levistock 行情字段（价格、涨跌幅、换手率等）
- [ ] 概念数据获取与展示
- [ ] 涨停原因抓取
- [ ] 国家大基金持股标识

### 8.4 第四阶段：功能增强
- [ ] 涨停原因归类分析
- [ ] 数据筛选功能
- [ ] 走势分析图表
- [ ] 智能推荐（基于板块热度）

### 8.5 第五阶段：测试优化
- [ ] 功能测试
- [ ] 性能优化
- [ ] 用户体验优化

---

## 九、关键代码示例

### 9.1 完整数据更新

```python
def update_all_data():
    """更新所有数据"""
    # 1. 获取热榜
    hot_stocks = fetch_hot_rank()
    codes = [s['code'] for s in hot_stocks]
    
    # 2. 获取板块
    sector_map = fetch_sector_info(codes)
    
    # 3. 获取行情
    spot_map = fetch_spot_data(codes)
    
    # 4. 获取概念
    concept_map = fetch_concepts(codes)
    
    # 5. 获取涨停信息
    limit_up_map = {}
    for code in codes[:20]:  # 只获取前20的涨停信息
        limit_up_map[code] = fetch_limit_up_info(code)
    
    # 6. 合并写入数据库
    for stock in hot_stocks:
        code = stock['code']
        data = {
            'code': code,
            'name': stock['name'],
            'rank': stock['rank'],
            'sector': sector_map.get(code, ''),
            'concept': concept_map.get(code, ''),
            **spot_map.get(code, {}),
            **limit_up_map.get(code, {})
        }
        save_to_db(data)
```

### 9.2 前端排序实现

```javascript
let currentData = [];
let sortColumn = null;
let sortDirection = 'asc';

function sortData(column) {
    if (sortColumn === column) {
        sortDirection = sortDirection === 'asc' ? 'desc' : 'asc';
    } else {
        sortColumn = column;
        sortDirection = 'asc';
    }
    
    currentData.sort((a, b) => {
        let va = a[column], vb = b[column];
        if (typeof va === 'string') va = va.toLowerCase();
        if (typeof vb === 'string') vb = vb.toLowerCase();
        return sortDirection === 'asc' ? (va < vb ? -1 : 1) : (va > vb ? -1 : 1);
    });
    
    renderTable();
}
```

---

## 十、注意事项

### 10.1 数据更新策略
- **热度排名**：每小时更新（通过定时任务）
- **实时行情**：每5分钟更新（页面刷新时）
- **涨停原因**：每日收盘后更新
- **板块概念**：每日更新一次

### 10.2 性能考虑
- 使用缓存减少API调用
- 分页加载大量数据
- 数据库索引优化
- 异步获取涨停原因（避免阻塞）

### 10.3 风险提示
- 股市有风险，数据仅供参考
- 涨停原因分析基于历史数据，不构成投资建议
- 同花顺API可能有访问频率限制

### 10.4 数据来源声明
- 热度数据来自同花顺官方API
- 行情数据来自东方财富（通过levistock）
- 板块概念数据来自东方财富
- 涨停原因等字段来自同花顺页面

---

## 十一、问题排查记录

### 11.1 thsdk 需要登录无法使用

- **问题**：部署 `https://github.com/panghu11033/thsdk` 后发现需要同花顺账号登录才能获取数据
- **影响**：无法自动化采集数据
- **解决**：删除 thsdk，改用 `https://github.com/fleetinglife/levistock` 项目，其 `stock_hot_rank_ths()` 接口无需登录即可获取同花顺热榜数据
- **经验**：选用第三方数据源时，优先选择无需认证的接口

### 11.2 全量A股数据过大

- **问题**：最初采集所有A股（5264只）数据量过大，CSV文件难以管理
- **影响**：数据加载慢、无法快速查看
- **解决**：改用 SQLite 数据库存储 + Flask Web 界面展示，只显示 TOP50，其余数据存库备查
- **经验**：大数据量场景必须用数据库，不要用 CSV/Excel

### 11.3 板块数据为空（0条记录）

- **问题**：数据库中 `sector` 字段全部为空，板块热度分析页面无数据
- **原因**：旧版 `web_server.py` 中 `do_update()` 虽然调用了 `lk.sector_stock_belong_em()`，但接口返回格式与代码解析不匹配，导致 `sector_map` 始终为空字典
- **排查过程**：
  1. 编写 `check_db.py` 检查数据库，发现 `有sector的股票: 0`
  2. 编写 `test_sector.py` 单独测试接口，确认 `sector_stock_belong_em()` 能正常返回数据
  3. 发现返回格式为 `[{'stock_code': '600584', 'sector_name': '半导体'}, ...]`
  4. 旧代码中 `except` 静默吞掉了异常，导致错误不可见
- **解决**：编写独立的 `update_with_sector.py` 脚本，正确解析返回数据并更新数据库，成功填充100只股票的板块信息
- **经验**：
  - 不要用裸 `except`，至少用 `except Exception as e` 并打印错误
  - 数据接口返回格式可能变化，需要打印原始返回值确认

### 11.4 数据库表结构升级后字段缺失

- **问题**：V2版本新增了 `concept`、`price`、`change_pct` 等字段，但 `CREATE TABLE IF NOT EXISTS` 不会修改已存在的表结构，导致查询报错 `sqlite3.OperationalError: no such column: s.concept`
- **影响**：页面显示"加载失败"
- **排查过程**：
  1. 查看服务器日志发现 `sqlite3.OperationalError: no such column: s.concept`
  2. 确认 `init_db()` 使用了 `CREATE TABLE IF NOT EXISTS`，表已存在时不会执行
  3. SQLite 不支持 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`
- **解决**：在 `init_db()` 中增加表迁移逻辑：
  ```python
  # 先检查表是否存在
  c.execute("SELECT name FROM sqlite_master WHERE type='table' AND name='stocks'")
  if table_exists:
      # 获取已有列名
      c.execute('PRAGMA table_info(stocks)')
      existing_cols = [col[1] for col in c.fetchall()]
      # 逐个添加缺失字段
      for col_name, col_type in new_cols.items():
          if col_name not in existing_cols:
              c.execute(f'ALTER TABLE stocks ADD COLUMN {col_name} {col_type}')
  ```
- **经验**：SQLite 的 `CREATE TABLE IF NOT EXISTS` 只在表不存在时创建，已有表需要用 `ALTER TABLE` 添加新字段

### 11.5 浏览器缓存导致页面不更新

- **问题**：重启 Web 服务器后，浏览器仍显示旧版本页面（白色主题），即使按 F5 也无效
- **原因**：浏览器缓存了旧的 HTML 页面
- **解决**：
  1. 先用 `taskkill /F /IM python.exe` 杀掉所有 Python 进程
  2. 重新启动 `web_server_v2.py`
  3. 在浏览器中访问 `http://localhost:5000/?nocache=1` 绕过缓存
- **经验**：开发阶段可在 Flask 响应头中添加 `Cache-Control: no-cache`

### 11.6 Web 服务器端口冲突

- **问题**：启动新版本服务器时，旧版本仍占用 5000 端口
- **现象**：新服务器启动成功但实际运行的仍是旧代码
- **解决**：启动前先执行 `taskkill /F /IM python.exe` 杀掉所有 Python 进程，再启动新版本
- **经验**：可用 `netstat -ano | findstr :5000` 确认端口占用情况

### 11.7 实时行情数据非交易时间为空

- **问题**：点击"立即更新"后，价格、涨跌幅等字段显示"-"
- **原因**：`lk.stock_zh_a_spot_em()` 获取的是实时行情，非交易时间（周末、收盘后）返回的数据中最新价等为 0 或空
- **解决**：这是正常现象，交易时间（工作日 9:30-15:00）更新即可获取实时数据。非交易时间显示的板块、排名等历史数据不受影响
- **经验**：金融数据接口通常只在交易时间返回有效行情数据

### 11.8 概念数据获取困难（已解决）

- **问题**：levistock 的 `sector_stock_belong_em()` 只返回行业板块，不返回概念标签，导致概念热度分析页面无数据
- **尝试方案**：
  - ~~`stock_individual_info_em(code)`：逐个查询太慢，且有频率限制~~
  - ~~同花顺页面爬虫（`https://ms.10jqka.com.cn/thsHotDetail/`）：JS渲染页面，直接请求HTML拿不到数据~~
  - ~~东方财富F10接口（`datacenter-web.eastmoney.com`）：参数复杂，返回格式不稳定~~
- **最终解决方案**：发现同花顺热榜API本身就返回了概念标签和涨停原因！无需额外爬虫
- **关键发现**：同花顺热榜API `https://dq.10jqka.com.cn/fuyao/hot_list_data/out/hot_list/v1/stock` 返回的每条股票数据中包含：
  ```json
  {
    "code": "600584",
    "name": "长电科技",
    "order": 1,
    "tag": {
      "concept_tag": ["国家大基金持股", "存储芯片", "先进封装"]
    },
    "analyse_title": "先进封装+CPO+央企",
    "analyse": "行业原因：据新华社... 公司原因：1、...",
    "rise_and_fall": 10.0027
  }
  ```
  - `tag.concept_tag` → 概念标签数组
  - `analyse_title` → 涨停原因标题（简短）
  - `analyse` → 涨停原因详情（行业原因+公司原因）
  - `rise_and_fall` → 涨跌幅（API直接返回，非交易时间也有值）
- **代码修改**：在 `do_update()` 中直接从热榜数据提取概念和涨停原因：
  ```python
  for stock in stocks:
      tag = stock.get('tag') or {}
      concept_tags = tag.get('concept_tag') or []
      if concept_tags:
          concept_map[code] = ','.join(concept_tags)
      
      analyse_title = stock.get('analyse_title') or ''
      if analyse_title:
          reason_title_map[code] = analyse_title
      
      api_change = stock.get('rise_and_fall')
      if api_change is not None:
          api_change_map[code] = float(api_change)
  ```
- **经验**：
  - 先仔细查看API返回的完整数据结构，可能已包含所需字段
  - 用 `json.dumps(data, ensure_ascii=False, indent=2)` 打印完整返回值排查
  - 编写 `test_concept_v2.py` 测试脚本逐个验证接口

---

## 十二、开发修改日志

### 12.1 [2026-05-25] V2版本 - 暗色系UI + 完整数据字段

**修改文件**：`web_server_v2.py`（新建）

**修改内容**：
1. UI改为暗色系扁平化设计（深蓝背景 #0f0f23）
2. "刷新"和"立即更新"按钮放到同一行
3. 新增"概念热度分析"Tab
4. 表格增加列排序功能（点击表头排序）
5. 数据库表结构扩展（新增concept、price、change_pct等14个字段）
6. 增加数据库迁移逻辑（ALTER TABLE添加缺失字段）
7. 整合levistock实时行情数据（stock_zh_a_spot_em）

**新增字段**：concept, price, change_pct, change_amt, volume, turnover, turnover_rate, pe_ratio, market_cap, limit_up_reason, national_fund, consecutive_boards, first_limit_time, seal_amount

### 12.2 [2026-05-25] 概念标签和涨停原因集成

**修改文件**：`web_server_v2.py`

**问题**：概念热度分析页面无数据，股票列表缺少概念标签和涨停原因

**排查过程**：
1. 用户指出概念热度分析没有内容，需要从同花顺热股详情页抓取
2. 用户提供了页面结构：`<div class="stock-abnormal-keyword">先进封装+CPO+央企</div>` 和 `<span aria-label="行业原因：...">`
3. 编写 `test_ths_detail.py` 测试直接请求页面HTML → 失败（JS渲染，HTML中无数据）
4. 编写 `test_concept_api.py` 测试东方财富F10接口 → 部分成功但参数复杂
5. 编写 `test_concept_v2.py` 测试同花顺热榜API返回字段 → **发现API已包含所有需要的数据**

**解决方案**：从同花顺热榜API直接提取，无需额外爬虫

**代码修改**：
1. `do_update()` 函数新增概念标签提取逻辑：
   - `tag.concept_tag` → concept字段（逗号分隔）
   - `analyse_title` → limit_up_reason字段（涨停原因标题）
   - `national_fund` → 检查概念标签中是否含"大基金"
   - `rise_and_fall` → change_pct字段（API直接返回涨跌幅）
2. 前端表格列从"换手率"改为"涨停原因"
3. 概念标签显示增加"国家队"标识（红色标签）
4. 概念标签最多显示3个（之前只显示2个）

**验证结果**：
- 概念热度分析页面正常显示：存储芯片(16)、CPO(13)、PCB(11)、国家大基金持股(9)、先进封装(8)
- 股票列表显示概念标签和涨停原因
- 涨跌幅数据正常显示（API直接返回，非交易时间也有值）

---

## 十三、待解决问题与开发计划

### 13.1 股票价格/涨跌幅非交易时间无数据

**问题描述**：
- 当前使用 `lk.stock_zh_a_spot_em()` 获取实时行情，非交易时间（收盘后、周末）返回的价格和涨跌幅为0或空
- 同花顺热榜API虽然返回 `rise_and_fall` 字段，但只反映热度排名时刻的涨跌幅，不是实时行情
- 用户需要全天候可用的价格/涨跌幅数据

**已尝试方案**：
- 同花顺热榜API的 `rise_and_fall` 字段：有值但非实时
- levistock的 `stock_zh_a_spot_em()`：非交易时间无数据

**计划解决方案**：
1. 从GitHub搜索其他同花顺实时API项目
2. 寻找支持盘后数据的API源
3. 考虑使用同花顺PC客户端API或WebSocket接口

**相关资源**：
- GitHub搜索：`https://github.com/search?q=同花顺&type=repositories&s=updated&o=desc`

### 13.2 股票上榜天数统计模块（开发中）

**需求描述**：
- 统计每只股票进入TOP100的持续天数
- 统计30天内总共进入前100的次数
- 点击股票行显示30天历史变化趋势
- 这些数据作为选股的重要指标

**功能设计**：
1. **上榜天数统计表**：
   - 股票代码、名称
   - 当前连续上榜天数
   - 30天内上榜总次数
   - 平均排名
   - 排名趋势（上升/下降）

2. **历史趋势弹窗**：
   - 点击股票行展开
   - 显示30天排名变化折线图
   - 显示每日涨跌幅
   - 标记连续上榜区间

3. **选股指标计算**：
   - 上榜持续性评分（连续天数权重高）
   - 上榜频率评分（30天内次数）
   - 排名稳定性评分（排名波动幅度）

### 13.3 股票推荐系统（开发中）

**需求描述**：
- 建立推荐股票板块
- 推荐逻辑基于多维度指标：
  1. 行业概念热度
  2. 上榜天数/趋势
  3. 板块支撑强度
  4. 同概念梯队数量

**推荐算法设计**：

```python
def calculate_recommend_score(stock):
    """
    推荐评分算法（满分100分）
    """
    score = 0
    
    # 1. 概念热度分（25分）
    concept_hot = get_concept_hot_score(stock.concept)  # 所属概念的平均热度
    score += concept_hot * 0.25
    
    # 2. 上榜持续性分（25分）
    continuous_days = stock.continuous_days  # 连续上榜天数
    score += min(continuous_days / 5, 1) * 25  # 5天满分
    
    # 3. 上榜频率分（20分）
    monthly_count = stock.monthly_count  # 30天内上榜次数
    score += min(monthly_count / 10, 1) * 20  # 10次满分
    
    # 4. 板块支撑分（15分）
    sector_support = get_sector_support_score(stock.sector)  # 板块内上榜股票数
    score += sector_support * 0.15
    
    # 5. 概念梯队分（15分）
    concept_team_size = get_concept_team_size(stock.concept)  # 同概念上榜股票数
    score += min(concept_team_size / 5, 1) * 15  # 5只满分
    
    return score
```

**推荐等级划分**：
- 90-100分：强烈推荐（五星）
- 80-89分：推荐（四星）
- 70-79分：关注（三星）
- <70分：观察

**推荐页面设计**：
1. 推荐股票列表（按评分排序）
2. 推荐理由展示（各项得分明细）
3. 板块/概念筛选器
4. 历史推荐准确率统计

---

## 十四、开发修改日志（续）

### 14.1 [2026-05-25] 问题记录：价格涨跌幅数据缺失

**问题**：非交易时间股票价格/涨跌幅显示为"-"或0

**原因分析**：
1. `lk.stock_zh_a_spot_em()` 只在交易时间返回有效数据
2. 同花顺热榜API的 `rise_and_fall` 是排名时刻的快照，非实时行情
3. 需要寻找支持盘后数据的API源

**下一步**：从GitHub搜索同花顺相关开源项目，寻找实时行情API方案

**解决方案（已实施）**：
1. 同花顺热榜API本身返回 `rise_and_fall` 字段（涨跌幅），非交易时间也有值 → 作为主要数据源
2. `akshare.stock_zh_a_hist()` 可获取最近交易日收盘数据 → 作为备选
3. `lk.stock_zh_a_spot_em()` 只在交易时间有效 → 保留作为实时数据源

### 14.2 [已完成] 股票上榜统计模块

**修改文件**：`web_server_v2.py`

**问题**：`hot_rank_history` 表缺少 `price` 和 `change_pct` 列

**排查过程**：
1. API返回500错误：`sqlite3.OperationalError: no such column: price`
2. 检查发现 `hot_rank_history` 表只有 `id, code, date, rank, created_at` 五列
3. 原因：`CREATE TABLE IF NOT EXISTS` 不会给已存在的表添加新列

**解决方案**：在 `init_db()` 中增加 `hot_rank_history` 表的迁移逻辑，用 `ALTER TABLE` 添加缺失列

**新增功能**：
- [x] `/api/stock_history/<code>` 接口：返回30天排名历史 + 统计指标
- [x] 统计指标：连续上榜天数、30天上榜次数、平均排名、最佳排名、排名趋势
- [x] 前端热度排名表格增加"上榜天数"列
- [x] 点击股票行展开30天历史面板（CSS柱状图 + 统计卡片）

**验证结果**：
- 600584（长电科技）：连续上榜30天，30天内上榜31次，平均排名40.7，最佳排名1

### 14.3 [已完成] 股票推荐系统

**修改文件**：`web_server_v2.py`

**新增功能**：
- [x] `/api/recommendations` 接口：返回推荐股票列表（按评分排序）
- [x] 推荐评分算法（满分100分）：
  - 概念热度分(25分)：所属概念中有多少个进入TOP100概念榜
  - 上榜持续性(25分)：连续上榜天数 / 5 * 25
  - 上榜频率(20分)：30天内上榜次数 / 10 * 20
  - 板块支撑(15分)：同板块有多少只进入TOP100
  - 概念梯队(15分)：同概念有多少只进入TOP100
- [x] 前端新增"推荐股票"Tab（第4个Tab）
- [x] 推荐列表显示：排名、股票、评分（星级）、上榜天数、30天次数、板块支撑、概念梯队、推荐理由
- [x] 星级规则：90-100五星、80-89四星、70-79三星、<70二星

**验证结果**：
- 推荐TOP5：长电科技(91.7)、华天科技(91.7)、通富微电(91.7)、兆易创新(91.7)、中芯国际(91.7)

### 14.4 [2026-05-25] 数据库迁移问题

**问题**：`hot_rank_history` 表缺少 `price` 和 `change_pct` 列

**原因**：SQLite 的 `CREATE TABLE IF NOT EXISTS` 只在表不存在时创建，已有表不会自动添加新列

**解决方案**：在 `init_db()` 中对所有表都增加迁移逻辑：
```python
c.execute("SELECT name FROM sqlite_master WHERE type='table' AND name='hot_rank_history'")
hist_exists = c.fetchone() is not None
if hist_exists:
    c.execute('PRAGMA table_info(hot_rank_history)')
    hist_cols = [col[1] for col in c.fetchall()]
    for col_name, col_type in {'price': 'REAL', 'change_pct': 'REAL'}.items():
        if col_name not in hist_cols:
            c.execute(f'ALTER TABLE hot_rank_history ADD COLUMN {col_name} {col_type}')
```

**经验**：每次修改表结构时，都要同时更新 `init_db()` 的迁移逻辑，确保旧数据库也能平滑升级

### 14.5 [2026-05-25] 前端页面显示不正常 - JavaScript SyntaxError

**问题**：前端页面只显示标题和Tab按钮，数据表格区域空白，无法调用任何API

**排查过程**：
1. 所有后端API通过curl/PowerShell验证均返回200和正确数据
2. HTML结构正确（5个Tab、content div初始为空）
3. 浏览器控制台报 `SyntaxError: Unexpected string`，导致整个 `<script>` 块解析失败
4. `loadData()` 从未执行，因此没有任何API请求发出
5. 用Node.js `--check` 做二分法定位，结果矛盾（部分行段通过但某些行段失败）
6. 最终判断为Python三引号字符串（`'''`）中嵌入HTML/JS时可能引入不可见字符或编码问题

**解决方案**：
1. 将HTML模板从Python三引号字符串中提取为独立的 `templates/index.html` 文件
2. 将 `render_template_string(HTML_TEMPLATE, ...)` 改为 `render_template('index.html', ...)`
3. Flask自动在应用同级目录的 `templates/` 子目录查找模板文件
4. 彻底避免了Python字符串编码对JavaScript的影响

**修改文件**：
- `web_server_v2.py`：删除 `HTML_TEMPLATE = '''...'''` 块（678行），改用 `render_template`
- 新建 `templates/index.html`：纯UTF-8编码的HTML文件

### 14.6 [2026-05-25] 连续上榜天数始终为0

**问题**：所有股票的"上榜天数"列显示"-"（即 `continuous_days = 0`）

**原因分析**：
- `get_continuous_days_bulk()` 函数使用 `datetime.now()` 作为起始日期
- 当当前时间已过零点（如凌晨00:30），`datetime.now()` 返回的是第二天日期
- 而数据库中最新数据是前一天（如2026-05-25），从第二天（2026-05-26）开始检查自然没有数据
- 同样的问题存在于 `get_stock_history()` 和 `get_recommendations()` 中的连续天数计算

**解决方案**：
将所有连续天数计算的起始日期从 `datetime.now()` 改为数据库中最新数据日期：
```python
# 修改前
check_date = datetime.now()

# 修改后
c.execute('SELECT MAX(date) FROM hot_rank_history')
latest_date = c.fetchone()[0]
check_date = datetime.strptime(latest_date, '%Y-%m-%d')
```

**影响范围**：3处修改
1. `get_continuous_days_bulk()` - 批量获取连续天数（表格显示用）
2. `get_stock_history()` - 单只股票历史统计（展开面板用）
3. `get_recommendations()` - 推荐系统评分计算

### 14.7 [2026-05-25] 排序图标显示为 \u2195 文本

**问题**：表头排序图标显示为 `\u2195` 文本而非 ↕ 符号

**原因**：HTML文件中JavaScript字符串 `\\u2195` 被当作字面量反斜杠+u2195，而非Unicode转义

**解决方案**：将 `templates/index.html` 中的 `'\\u2195'` 改为 `'\u2195'`

### 14.8 [2026-05-25] 项目结构整理 - GitHub上传准备

**修改内容**：

1. **路径改为相对路径**（兼容GitHub部署）：
   - `web_server_v2.py`：`DB_PATH` 和 `REPORT_DIR` 改为 `Path(__file__).resolve().parent / 'hot_rank.db'`
   - `report_generator.py`：同上
   - 删除了 `sys.path.insert(0, ...)` 硬编码路径

2. **新增文件**：
   - `.gitignore`：忽略 `*.db`、`*.csv`、`reports/`、`__pycache__/`、`venv/` 等
   - `requirements.txt`：flask>=3.0, levistock>=1.0, akshare>=1.14, requests>=2.31
   - `README.md`：中文项目说明，包含功能特性、安装运行方法

3. **最终项目结构**：
```
.
├── .gitignore
├── README.md
├── requirements.txt
├── web_server_v2.py      # Flask主程序
├── report_generator.py   # 报告生成器
├── templates/
│   └── index.html        # 前端模板
├── reports/              # 生成报告（.gitignore忽略）
├── hot_rank.db           # 数据库（.gitignore忽略）
└── 开发文档.md
```

### 14.18 [2026-05-26] 排序图标乱码修复+前端模块化重构

**问题**：每个表头后面显示"2191"/"2195"乱码，实际是Unicode字符 `\u2195`（↕）在某些编码下显示异常

**修复**：
- JS中 `'\u2195'` 改为实际Unicode字符 `'⇅'`（U+21C5）
- CSS中 `content: "\\2191"` 改为 `content: " ▲"`，`"\\2193"` 改为 `" ▼"`

**前端模块化重构**：
1. 新增 `tabDataCache` 对象：每个Tab独立缓存数据，切换Tab不重新请求
2. 新增 `tabLoading` 对象：防止同一Tab重复请求
3. 新增 `renderTabContent()` 函数：将渲染逻辑从 `loadData()` 中提取出来
4. 重写 `loadData()`：先检查缓存 → 有缓存直接渲染 → 无缓存才请求API
5. 刷新按钮/日期切换：清空所有缓存后重新加载
6. 自动刷新：只刷新当前活动Tab的缓存，不影响其他Tab

**松耦合设计**：
- 各Tab数据独立加载、独立缓存、独立渲染
- 切换Tab不会触发其他Tab的数据刷新
- 后台数据更新只影响当前可见Tab

**错误记录**：
- `\u2195` 在HTML的 `<script>` 标签中可能被错误解析为字符编码而非Unicode转义
- 使用实际Unicode字符（⇅▲▼）比转义序列更可靠

### 14.17 [2026-05-26] 排版错位彻底修复（第三次迭代）

**问题根源**：多次尝试修复排版但反复出现错位，根本原因是：
1. `table-layout: fixed` + `width: 100%` 导致列宽被强制压缩，8列放不下
2. 概念标签用 `inline-block` 排列，长标签（如"国家大基金持股"7字）撑开单元格
3. 没有水平滚动机制，表格超出容器宽度时内容被截断或挤压

**尝试历史**：
- 第一次：加 `table-layout: fixed` + nth-child固定列宽 → 表头被截断（"排..."）
- 第二次：去掉 fixed，用 min-width 约束 → 概念标签仍然溢出
- 第三次（最终方案）：**彻底重写表格CSS**

**最终方案**：
1. `#content` 加 `overflow-x: auto` — 表格超宽时出现水平滚动条
2. `table` 去掉 `width: 100%`，让表格按内容自然撑开
3. `table` 加 `white-space: nowrap` — 防止单元格内容意外换行
4. 概念列和涨停原因列单独设 `white-space: normal` 允许标签换行
5. 概念标签容器用 `flex-wrap: wrap` + `max-height: 52px` + `overflow: hidden`
6. 涨停原因列用 `max-width: 200px` + `text-overflow: ellipsis` + `title` 属性悬停查看全文
7. 每列用 `nth-child` 设置合理的 `min-width`，确保最小可用宽度

**错误记录**：
- `table-layout: fixed` 会忽略内容实际宽度，强制按比例分配，导致窄列内容被截断
- 只给 `th` 设 `overflow: visible` 不够，`td` 的 `overflow: hidden` 仍然会截断内容
- `min-width` 在 `table-layout: fixed` 模式下不生效，必须去掉 fixed 才有效
- 概念标签的 `max-width: 80px` 对中文来说太窄（"国家大基金持股"需要约90px），但保持截断是合理的空间权衡

**技术分析超时优化**：
- 将技术分析总超时从30秒缩短到10秒，避免推荐API响应太慢导致前端"加载中"太久

4. **数据库说明**：SQLite是文件型数据库，天然兼容GitHub。但数据库文件较大（45万+记录），通过 `.gitignore` 排除。用户clone后首次运行会自动创建空数据库并更新数据。

### 14.9 [2026-05-25] 已知限制

1. **非交易时间价格显示"-"**：`lk.stock_zh_a_spot_em()` 只在交易时间返回有效数据。非交易时间只有涨跌幅（来自热榜API的 `rise_and_fall`），没有最新价。
2. **akshare备选方案**：已实现TOP10股票的akshare历史收盘价备选获取，但全量获取太慢（每只需单独请求），暂只对TOP10生效。

### 14.10 [2026-05-26] 推荐系统V3.0升级

**参考来源**：另一个同花顺热度榜选股程序的低价股精选报告（七因子模型）

**升级内容**：

#### 1. 评分模型从5因子升级为10因子

| 因子 | 权重 | 来源 | 说明 |
|------|:----:|------|------|
| 概念热度 | 20% | 原有（调权） | 所属概念中有多少个进入TOP100概念榜 |
| 上榜持续性 | 20% | 原有（调权） | 连续上榜天数 |
| 上榜频率 | 15% | 原有（调权） | 30天内上榜次数 |
| 板块支撑 | 15% | 原有 | 同板块有多少只进入TOP100 |
| 概念梯队 | 10% | 原有（调权） | 同概念有多少只进入TOP100 |
| **上升速度** | **10%** | **新增** | 排名较前一天的上升幅度 |
| **价格优势** | **5%** | **新增** | 低价股偏好（5元以下满分） |
| **连板溢价** | **3%** | **新增** | 连板天数越多溢价越高 |
| **技术信号** | **2%** | **新增** | 涨停/强势形态识别 |
| **流动性** | **0基准** | **新增** | 流通市值50-500亿最优，偏离扣分 |

#### 2. 新增风险评级系统

| 风险等级 | 条件 | 扣分 |
|----------|------|------|
| 追高风险 | 涨幅≥9.5%（涨停） | 0（标记但不扣分） |
| 高风险 | 涨幅≥7% 或 ST股 | 2-5分 |
| 中风险 | 涨幅≥5% 或 跌幅≤-3% | 1-2分 |
| 低风险 | 其他 | 0 |

#### 3. 新增止损止盈建议
- 止损线：当前价 × 0.95（-5%）
- 止盈1档：当前价 × 1.05（+5%）
- 止盈2档：当前价 × 1.10（+10%）
- 仅在有价格数据时显示

#### 4. 新增大盘情绪指数
- 基于TOP100涨跌分布计算
- 情绪指数 = (上涨占比×50) + (涨停占比×30) + 20
- 范围0-100，越高越乐观
- 新增API：`/api/sentiment`

#### 5. 新增自动轮询更新
- 前端每60秒检查一次后端更新状态
- 检测到新数据后自动刷新推荐Tab
- 新增API：`/api/update_status`

**修改文件**：
- `web_server_v2.py`：新增 `get_market_sentiment()`、`/api/sentiment`、`/api/update_status`，重写 `get_recommendations()`
- `templates/index.html`：重写 `renderRecommendTable()`，新增情绪指数显示、风险列、止损止盈列、自动轮询

**验证结果**：
- 推荐TOP1：通富微电(002156) 85.3分，追高风险
- 情绪指数：72.5（偏多），上涨80家，涨停40家

### 14.11 [2026-05-26] 排版错位修复

**问题**：表格列宽不固定，概念标签和涨停原因文字溢出导致列错位

**解决方案**：
1. 添加 `table-layout: fixed` 强制固定列宽
2. 给排名表和推荐表分别添加CSS class（`.rank-table`、`.rec-table`）
3. 用 `nth-child` 选择器为每列设置固定宽度
4. 概念列和涨停原因列设置 `white-space: normal` 允许换行
5. 所有单元格添加 `overflow: hidden; text-overflow: ellipsis` 防溢出

### 14.12 [2026-05-26] 推荐股票精简+过滤规则

**修改**：
- 推荐股票从100只精简为**最多8只**
- **价格过滤**：只保留价格<20元的股票（非交易时间price=0时不过滤）
- **交易所过滤**：排除科创板（688开头）、北交所（8开头或4开头且长度>=6）
- 不足8只时不补，有多少返回多少

### 14.13 [2026-05-26] 技术分析模块

**新增函数**：`get_technical_scores(codes)`
- 使用akshare获取近30天日K线数据
- 计算四个技术指标（总分0-6分）：
  - MA5 vs MA10（+2分）：短期均线在长期均线上方
  - MACD柱状图（+2分）：MACD>0且递增
  - 成交量趋势（+1分）：近5日均量>前5日均量
  - 价格vs MA20（+1分）：当前价在20日均线上方
- **超时保护**：总超时30秒，避免网络慢时阻塞太久
- **优化**：只对过滤后的候选股票计算（排除科创板/北交所），减少无效请求

### 14.14 [2026-05-26] 每日复盘模块

**新增函数**：`get_daily_review()`
- 获取昨日TOP5推荐股票
- 对比今日排名变化和涨跌幅
- 生成复盘报告：`summary`（文字总结）+ `lessons`（经验教训）
- 经验教训规则引擎：
  - 涨停股次日追高风险大 → 涨停股扣2分
  - 低价股表现优于高价股 → 低价股加1分
  - 热度排名下降股需警惕 → 排名下降股扣1分
  - 市场整体偏弱时谨慎 → 情绪指数<50时全盘扣1分
- **新增API**：`/api/review`

### 14.15 [2026-05-26] 股票价格不显示问题

**问题**：所有股票"最新价"显示"-"，数据库中price=0

**原因分析**：
1. `lk.stock_zh_a_spot_em()`（levistock）只在交易时间返回有效数据
2. 非交易时间该接口失败，备选方案只获取TOP10
3. 东方财富API返回502（网络代理问题）
4. akshare的 `stock_zh_a_spot_em()` 也连接失败

**解决方案**：新增**新浪财经API**作为价格数据源
- 新浪API优势：非交易时间也能返回最后收盘价、支持批量查询（每次50只）、响应快
- 新增函数 `fetch_sina_prices(codes)`：批量获取价格
- 数据获取优先级：levistock → 新浪财经 → akshare单只备选
- 手动运行脚本更新了112只股票的价格数据

**错误记录**：
- 东方财富 push2 API 返回502 Bad Gateway（网络限制）
- akshare `stock_zh_a_spot_em()` 报 `RemoteDisconnected`（网络限制）
- 新浪API第一批（前50只）超时（timeout=10s太短），改为30s后成功
- 临时目录和用户目录的数据库文件不同步，导致API返回旧数据（需重启服务器）

### 14.16 [2026-05-26] GitHub上传

**状态**：代码已本地git commit，推送失败（网络无法连接github.com:443）
**仓库地址**：`https://github.com/yjj79819/tdx`
**待解决**：需要配置网络代理才能推送

**项目最终结构**：
```
.
├── .gitignore
├── README.md
├── requirements.txt
├── web_server_v2.py          # Flask主程序（推荐系统V3.0+技术分析+复盘）
├── report_generator.py       # Markdown报告生成器
├── templates/
│   └── index.html            # 前端模板（暗色主题+colgroup列宽）
├── reports/                  # 每日推荐报告（.gitignore忽略）
├── hot_rank.db               # SQLite数据库（.gitignore忽略）
└── 开发文档.md
```

### 14.19 [2026-05-26] 表头对齐终极修复 - colgroup方案

**问题根源**：CSS nth-child列宽在不同浏览器中表现不一致，导致表头和数据列错位。
用户浏览器（可能是Chrome/Edge的某个版本）中，nth-child的列宽计算与我的测试环境不同。

**尝试历史**（共5次迭代）：
1. `table-layout: fixed` + nth-child固定列宽 → 表头文字被截断
2. 去掉fixed，用min-width约束 → 概念标签仍然溢出
3. `table { white-space: nowrap }` + 部分列 `white-space: normal` → nowrap冲突导致列宽异常
4. 去掉table级nowrap，每列独立控制 → 我的浏览器OK，用户浏览器仍然错位
5. **最终方案：`<colgroup>` + `<col>`**（本次）

**最终方案**：
- 在 `<table>` 后立即插入 `<colgroup>` 定义每列宽度
- 热度排名表：40+100+65+70+60+70+150+150 = 705px
- 推荐表：40+100+55+65+60+60+100+140 = 620px
- 完全删除所有CSS nth-child列宽规则
- `<colgroup>` 是HTML标准，所有浏览器都支持，列宽绑定在表格结构上而非CSS选择器

**其他修改**：
- 清除ST股票（*ST闻泰、ST洲际），后端 `fetch_hot_rank()` 自动过滤含"ST""退"的股票
- 精简展开面板：去掉CSS柱状图、渐变色、装饰线条，只保留纯文字统计数据
- 回填20天历史数据（同花顺API支持 `date=YYYY-MM-DD` 参数）
- 修复连续上榜天数计算：只看交易日而非日历天
- 排序null值处理：防止空值导致排序错乱
- 30天滚动保留：每次更新后自动清理第31天前数据

**错误记录**：
- CSS nth-child列宽在不同浏览器/分辨率下计算结果不同，不可靠
- `table { white-space: nowrap }` 与子元素 `white-space: normal` 冲突
- `table-layout: fixed` 会忽略内容实际宽度，导致窄列截断
- `<colgroup>` 的 `<col style="width:...">` 是最可靠的跨浏览器列宽方案
- 同花顺热榜历史API：`https://dq.10jqka.com.cn/...&date=YYYY-MM-DD`（只返回排名，不含概念/板块等详细信息）

### 14.20 [2026-05-26] 服务端禁缓存+板块概念列加宽+推荐表增加价格列

**问题**：用户浏览器缓存旧版HTML，导致colgroup修复未生效，表头仍然错位

**修复**：
- Flask返回主页时添加 `Cache-Control: no-cache, no-store, must-revalidate` 响应头
- 彻底禁止浏览器缓存HTML页面

**列宽调整**：
- 热度排名表：板块列 70→100px，概念列 150→200px，涨停原因列 150→160px
- 推荐表：新增"最新价"列（55px），共9列

**推荐表增强**：
- 新增"最新价"列，显示当前股价（如 ¥6.92）
- 去掉评分星星，只保留数字评分
- 风险等级字体缩小到12px

### 14.21 [2026-05-26] 推荐逻辑修复+数据全量补全

**问题**：
1. 推荐股票出现天赐材料(¥53)，价格过滤逻辑有bug：`not r.get('price') or r['price'] <= 0` 导致price=0的股票全部通过
2. 板块分析只有2个板块，1272只股票中只有8只有板块信息
3. 1255只股票price=0（回填历史数据时被INSERT OR REPLACE覆盖）

**修复**：
1. **推荐过滤**：改为 `r.get('price') and r['price'] > 0 and r['price'] < 20`，必须有有效价格且<20元
2. **板块信息**：用 `lk.sector_stock_belong_em(codes)` 分批获取（每批100只），1272只全部获取成功
3. **实时行情**：用新浪财经API `hq.sinajs.cn` 批量获取（每批50只，需Referer头），1223只有价格
4. **table-layout: fixed**：强制浏览器按colgroup分配列宽，彻底解决表头错位

**数据补全结果**：
- 板块信息：8 → 1272只（半导体139只、电力69只、通用设备43只...）
- 实时行情：17 → 1223只
- 价格<20元：496只
- 板块分析：2个 → 39个板块

**错误记录**：
- `not 0` 在Python中为True，导致price=0的股票绕过价格过滤
- 新浪财经API需要 `Referer: https://finance.sina.com.cn/` 头，否则返回Forbidden
- akshare的 `stock_zh_a_spot_em()` 网络不通时，新浪API是可靠的备用方案
- 回填历史数据的 `INSERT OR REPLACE` 会覆盖stocks表已有字段（price、sector等），应该只插入hot_rank_history而不覆盖stocks

### 14.22 [2026-05-26] CSS Grid替代table布局 - 彻底解决表头错位

**问题**：用户反复反馈表头对不齐，尝试了5种方案都失败：
1. table-layout: fixed + colgroup
2. nth-child列宽
3. 服务端禁缓存
4. white-space控制
5. 各种组合

**根本原因**：`<table>` 元素在不同浏览器中的列宽计算逻辑不同，即使设置了colgroup和table-layout:fixed，浏览器仍会根据内容调整列宽。

**最终方案**：**放弃 `<table>`，改用 CSS Grid 布局**
- 每个单元格是独立的 `<div>`
- 列宽通过 `grid-template-columns` 绝对固定
- 表头和数据行都是Grid的子元素，共享同一套列宽定义
- 不受内容影响，不受浏览器差异影响

**Grid列宽定义**：
```css
.grid-table.rank-grid {
    grid-template-columns: 50px 120px 75px 80px 70px 110px 220px 180px;
    /* 排名 股票 最新价 涨跌幅 上榜天数 板块 概念 涨停原因 */
    /* 总宽: 905px */
}
```

**优点**：
- 列宽绝对固定，不会因内容变化而改变
- 表头和数据行天然对齐（都是Grid子元素）
- 跨浏览器一致（Grid是CSS标准，所有现代浏览器支持）
- 不需要colgroup、table-layout等hack

**缺点**：
- 语义上不如 `<table>` 清晰（但可通过ARIA属性补充）
- 不支持原生表格特性（如rowspan/colspan）

---

## 15. 错误教训总结（永久记忆）

### 15.1 表头对齐问题（折腾了6次）

**错误历程**：
1. `table-layout: fixed` → 表头文字被截断
2. CSS nth-child列宽 → 不同浏览器计算结果不同
3. `table { white-space: nowrap }` + 子元素 `white-space: normal` → nowrap冲突
4. 去掉table级nowrap，每列独立控制 → 我的浏览器OK，用户浏览器仍错位
5. `<colgroup>` + `<col>` → 用户浏览器缓存旧HTML，修复未生效
6. 服务端禁缓存 + `<colgroup>` → 用户浏览器仍错位（`<table>`本身的问题）

**根本原因**：`<table>` 元素在不同浏览器中的列宽计算逻辑不同，即使设置了colgroup和table-layout:fixed，浏览器仍会根据内容调整列宽。

**最终方案**：**放弃 `<table>`，改用 CSS Grid 布局**
- 列宽通过 `grid-template-columns` 绝对固定
- 表头和数据行都是Grid子元素，共享同一套列宽
- 不受内容影响，不受浏览器差异影响

**教训**：
- 不要用 `<table>` 做复杂布局，CSS Grid更可靠
- 用户浏览器缓存是常见问题，服务端必须禁缓存
- 不同浏览器CSS表现不同，必须用标准方案（Grid/Flex）

### 15.2 推荐过滤逻辑bug

**错误代码**：
```python
recommendations = [r for r in recommendations if not r.get('price') or r['price'] <= 0 or r['price'] < 20]
```

**问题**：`not 0` 在Python中为True，导致price=0的股票绕过价格过滤，天赐材料(¥53)被推荐。

**正确代码**：
```python
recommendations = [r for r in recommendations if r.get('price') and r['price'] > 0 and r['price'] < 20]
```

**教训**：
- Python中 `not 0` = True，`not None` = True，要区分这两种情况
- 过滤条件要明确：必须有有效值 且 满足条件

### 15.3 回填历史数据覆盖stocks表

**错误代码**：
```python
c.execute('''INSERT OR REPLACE INTO stocks (code, name, market, ...) VALUES (...)''', ...)
```

**问题**：`INSERT OR REPLACE` 会完全覆盖已有记录，导致stocks表的price、sector、concept等字段被清空。

**正确做法**：
- 回填历史数据时，只插入 `hot_rank_history` 表
- 不要覆盖 `stocks` 表已有字段
- 或者用 `INSERT OR IGNORE` + `UPDATE` 分开处理

**教训**：
- `INSERT OR REPLACE` 是完全覆盖，不是部分更新
- 历史数据回填要谨慎，不要破坏已有数据

### 15.4 数据库不同步

**问题**：运行目录的数据库只有98条记录，而主数据库有3231条。

**原因**：每次修改后只同步了代码文件，没有同步数据库文件。

**教训**：
- 数据库文件必须和代码一起同步
- 启动前检查数据库是否最新
- 可以用软链接或配置统一数据库路径

### 15.5 板块信息缺失

**问题**：1272只股票只有8只有板块信息。

**原因**：回填历史数据时覆盖了stocks表，板块信息被清空。

**解决**：用 `lk.sector_stock_belong_em(codes)` 批量获取板块信息。

**教训**：
- 板块信息是重要维度，必须保证完整性
- levistock的 `sector_stock_belong_em()` 是高效批量获取方案

### 15.6 行情数据获取失败

**问题**：akshare的 `stock_zh_a_spot_em()` 网络不通。

**解决**：用新浪财经API `hq.sinajs.cn` 作为备用，需要加 `Referer: https://finance.sina.com.cn/` 头。

**教训**：
- 网络API要有备用方案
- 新浪财经API需要Referer头，否则返回Forbidden
- 批量获取时控制并发（每批50只，间隔200ms）

### 15.7 连续上榜天数计算错误

**问题**：周末导致连续上榜天数中断。

**原因**：代码按日历天逐天检查，遇到周末就断了。

**解决**：只检查交易日（数据库中存在的日期），而不是连续日历天。

**教训**：
- 股票数据要按交易日计算，不是日历天
- 周末、节假日要跳过

### 15.8 浏览器缓存问题

**问题**：修改代码后用户看到仍是旧版本。

**解决**：Flask返回HTML时加 `Cache-Control: no-cache, no-store, must-revalidate` 响应头。

**教训**：
- 开发阶段必须禁缓存
- 用户刷新不一定能清除缓存，服务端要主动控制

---

## 16. [2026-05-26] GitHub Pages部署 - 让程序在GitHub上运行

### 背景
用户要求程序能在GitHub上运行并访问，不需要本地服务器。

### 限制
- GitHub Pages只能托管静态文件，不能运行Python后端
- GitHub Actions可以定时运行Python脚本（免费版每4小时一次）
- 仓库必须是 **公开** 的才能使用免费版GitHub Pages

### 方案架构

```
GitHub Actions (每4小时)
    │ 运行 export_data.py
    │ 获取同花顺API数据
    ↓
生成 JSON 数据文件 → docs/data/*.json
    │
GitHub Pages 部署
    ↓
静态前端 → docs/index.html → 读取本地JSON
    │
用户访问: https://yjj79819.github.io/tdx/
```

### 创建的文件

| 文件 | 说明 |
|------|------|
| `docs/index.html` | 静态前端页面（从templates/index.html改造，全部读本地JSON） |
| `export_data.py` | 独立数据导出脚本（同花顺API→JSON文件） |
| `.github/workflows/update_data.yml` | GitHub Actions工作流（定时0 */4 * * *） |
| `docs/data/` | JSON数据文件目录（生成） |
| `docs/data/history/` | 个股历史数据目录（生成） |

### 静态前端改动要点
- 原Flask的 `/api/*` 改为读 `./data/*.json`
- 移除日期选择器、手动更新按钮、轮询逻辑
- 新增顶部通知横幅"数据每4小时自动更新"
- 保留所有功能：TOP50、板块分析、概念热榜、行业热榜、推荐、展开历史

### 部署步骤
1. 代码推送到GitHub
2. 仓库 Settings → Pages → Source选 **GitHub Actions**
3. 手动触发Actions工作流（或等4小时自动触发）
4. 访问 `https://yjj79819.github.io/tdx/`

### 教训
- GitHub Pages免费版不支持私有仓库，必须改为公开
- 可以通过GitHub REST API直接用Token操作（改仓库可见性、启用Pages、触发工作流）
- 修改前核实：先检查仓库状态（私有/公开），避免API报错后盲目排查
